import * as Conversion from '../utils/Conversion';
import * as Schedule from '../utils/Schedule';
import * as ERC712 from '../utils/ERC712';
import * as Constants from '../utils/Constants';

export const Utils = {
  constants: Constants,
  conversion: Conversion,
  erc712: ERC712,
  schedule: Schedule
}
