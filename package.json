{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
	"scripts": {
    "start": "./scripts/start.sh",
		"publish": "lerna run build && lerna publish"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
