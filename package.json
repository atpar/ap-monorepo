{
  "name": "root",
  "private": false,
  "devDependencies": {
    "ganache-cli": "^6.8.1",
    "lerna": "^3.13.0",
    "truffle": "^5.1.8"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "coverage": "./scripts/coverage.sh",
    "link-packages": "lerna link",
    "publish": "lerna run build && lerna publish --exact --dist-tag latest",
    "test": "./scripts/test.sh"
  },
  "publishConfig": {
    "access": "public"
  }
}
