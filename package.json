{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
	"scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "link-packages": "lerna link",
    "publish": "lerna run build && lerna publish --exact --dist-tag app",
    "start": "./scripts/start.sh",
    "test": "./scripts/test.sh"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
